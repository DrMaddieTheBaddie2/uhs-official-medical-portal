# UHS Medical Portal

Official UHS Medical Handbook, SOPs, Clinical Guidelines, Medications and Training Portal.

A single-page React application built with Vite, React 19, TypeScript and Tailwind CSS v4.

## Getting started

```bash
npm install
npm run dev       # http://localhost:5173
```

## Scripts

| Command           | Purpose                              |
| ----------------- | ------------------------------------ |
| `npm run dev`     | Start the Vite dev server            |
| `npm run build`   | Type-check (`tsc -b`) + production build to `dist/` |
| `npm run lint`    | Lint with oxlint                     |
| `npm run preview` | Serve the production build locally   |

## Project structure & conventions

See [CLAUDE.md](CLAUDE.md) for the folder structure, design-token system, coding standards and development workflow.
